import { APIGatewayEvent, Callback, Context, Handler } from 'aws-lambda';
import { DynamoDB } from 'aws-sdk';

export const checkMessage: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const sendMessage: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const seed: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const addWord: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const removeWord: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const getWords: Handler = (event: APIGatewayEvent, context: Context) => {
};

export const help: Handler = (event: APIGatewayEvent, context: Context) => {
};
